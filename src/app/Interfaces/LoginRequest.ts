export interface LoginRequest {
  email: string;
  password: string;
}
// This interface defines the structure of the login request object.
// It includes two properties: `email` and `password`, both of which are strings.